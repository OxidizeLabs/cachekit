\* FIFO TLC config — see fifo-tla-guide.md and fifo-tlc.md
SPECIFICATION Spec
CONSTANTS
    \* Small finite instance for state-space exploration
    Keys = {k1, k2}
    Capacity = 2
    NoVictim = NoVictim
    \* TLC pruning bound (stale queue growth after Remove); not fifo.md semantics
    MaxQueueLen = 12
\* Policy invariants only (excludes ExplorationOK queue cap from proof goal)
INVARIANT
    SemanticOK
CONSTRAINT
    QueueLengthBound
\* Exploration may stall when queue is full under MaxQueueLen
CHECK_DEADLOCK
    FALSE
